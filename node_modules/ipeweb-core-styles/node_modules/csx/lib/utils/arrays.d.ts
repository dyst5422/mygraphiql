import { List } from '../types';
export declare const filter: <T>(args: List<T>, condition: (item: T) => boolean) => T[];
export declare const map: <TInput, TOutput>(args: List<TInput>, mapper: (item: TInput) => TOutput) => TOutput[];
